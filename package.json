{
  "name": "intranet-saas-monorepo",
  "version": "0.1.0",
  "description": "Intranet SaaS monorepo with Next.js frontend, NestJS backend, and Prisma ORM",
  "private": true,
  "license": "GPL-3.0-or-later",
  "scripts": {
    "frontend:install": "npm --prefix frontend install",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:start": "npm --prefix frontend run start",
    "frontend:lint": "npm --prefix frontend run lint",
    "backend:install": "npm --prefix backend install",
    "backend:dev": "npm --prefix backend run dev",
    "backend:build": "npm --prefix backend run build",
    "backend:start": "npm --prefix backend run start:prod",
    "backend:lint": "npm --prefix backend run lint",
    "backend:test": "npm --prefix backend run test",
    "backend:prisma:generate": "npm --prefix backend run prisma:generate",
    "backend:prisma:migrate": "npm --prefix backend run prisma:migrate",
    "backend:prisma:migrate:deploy": "npm --prefix backend run prisma:migrate:deploy",
    "backend:seed": "npm --prefix backend run seed",
    "install:all": "npm run frontend:install && npm run backend:install",
    "dev": "echo 'Run frontend:dev and backend:dev in separate terminals'",
    "lint": "npm run frontend:lint && npm run backend:lint",
    "build": "npm run backend:build && npm run frontend:build",
    "test": "npm run backend:test"
  },
  "keywords": [
    "monorepo",
    "nextjs",
    "nestjs",
    "prisma",
    "saas",
    "intranet"
  ],
  "author": "victordg0223",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
